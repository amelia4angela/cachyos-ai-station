> [Home](README.md) | 部署指南：[0](阶段零-前置授权.md) → [1](阶段一-环境检测.md) → [2](阶段二-Hermes多代理系统.md) → [3](阶段三-网络代理.md) → [4](阶段四-输入法.md) → [5](阶段五-Sunshine串流.md) | 参考：[环境要求](环境要求.md) [游戏环境](游戏环境.md) [踩坑记录](踩坑记录.md)

---

# 阶段四：输入法

## 4.1 安装

```bash
# Fcitx5 输入法框架 + Rime 引擎
sudo pacman -S --needed --noconfirm fcitx5-im fcitx5-rime

# 雾凇拼音（AUR）
paru -S --needed --noconfirm rime-ice-git fcitx5-mellow-themes-git
```

### 额外主题

```bash
paru -S --needed --noconfirm \
  fcitx5-skin-material \
  fcitx5-skin-fluentdark-git \
  fcitx5-theme-dracula-git \
  fcitx5-themes-macos-git
```

## 4.2 环境变量

**只写 `~/.config/plasma-workspace/env/ime.sh`**，严禁写到 `.bashrc` / `.profile` / `/etc/environment`：

```bash
mkdir -p ~/.config/plasma-workspace/env
cat > ~/.config/plasma-workspace/env/ime.sh <<'EOF'
#!/bin/bash
export XMODIFIERS=@im=fcitx
EOF
chmod +x ~/.config/plasma-workspace/env/ime.sh
```

> Wayland 下 GTK/QT 模块由 fcitx5 自动处理，只需设 `XMODIFIERS`。

## 4.3 雾凇配置

```bash
mkdir -p ~/.local/share/fcitx5/rime

# 软链雾凇文件
ln -sfn /usr/share/rime-data/rime_ice.schema.yaml ~/.local/share/fcitx5/rime/
ln -sfn /usr/share/rime-data/rime_ice.dict.yaml ~/.local/share/fcitx5/rime/
ln -sfn /usr/share/rime-data/rime_ice_suggestion.yaml ~/.local/share/fcitx5/rime/

# 设置默认输入方案
cat > ~/.local/share/fcitx5/rime/default.custom.yaml <<'YAML_EOF'
patch:
  schema_list:
    - {schema: rime_ice}
    - {schema: luna_pinyin}
YAML_EOF
```

## 4.4 主题

Mellow 主题装完后软链到用户目录：

```bash
mkdir -p ~/.local/share/fcitx5/themes
ln -sf /usr/share/fcitx5/themes/mellow-* ~/.local/share/fcitx5/themes/
```

### 已安装的 29 个主题

| 风格 | 主题名 |
|---|---|
| Material Design | fcitx5-skin-material |
| Fluent Design | FluentDark, FluentDark-solid |
| Dracula | Dracula |
| macOS | MacOS-Dark, MacOS-Light |
| Mellow 系列（10个） | graphite/sakura/vermilion/wechat/youlan × light+dark |
| Mellow KWin 模糊（10个） | 同上带模糊效果 |
| 默认 | default, default-dark |

## 4.5 验证与启用

### 第一步：设置虚拟键盘

> ⚠️ 这一步必须做，否则 Fcitx5 不会生效！

1. 打开 **系统设置** → **键盘**
2. 找到 **虚拟键盘**（Virtual Keyboard）
3. 选择 **Fcitx5 (Wayland)**
4. 注销重新登录

### 第二步：配置输入法

```bash
fcitx5-diagnose  # 看输出有无错误
```

打开 `fcitx5-configtool`：
- 选 **全局配置** → 添加输入法 → 选 **Rime**
- 选 **附加组件** → 确认 Rime 已启用
- 选 **外观** → 选喜欢的主题

### 第三步：测试

按 `Ctrl+Space` 切换输入法，打字测试中文候选词是否正常。
