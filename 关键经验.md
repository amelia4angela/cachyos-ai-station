# 关键经验

1. **Linux Wayland + 串流 = 复杂** — X11 简单但 KDE 体验差
2. **portal 配置改了要重启 user session** — 不是 system
3. **多 portal impl 抢显示器** — 闪烁常见原因
4. **kscreen-doctor 必须从桌面环境调用** — 需要 Wayland/D-Bus 变量
5. **Sunshine global_prep_cmd 是 JSON 数组** — 不是简单命令
6. **DNS 污染** — SOCKS5 + remote_dns=true
7. **不要乱改 logind 配置** — 会触发会话重置
8. **card 编号可能在重启后交换** — 用 connector 名而不是 card 号
9. **kwinoutputconfig.json 的 ghost 条目** — 热插拔残留，需要清理
10. **D-Bus 服务文件名必须匹配 D-Bus 名** — 大小写敏感
