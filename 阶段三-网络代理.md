> [Home](README.md) | 部署指南：[0](阶段零-前置授权.md) → [1](阶段一-环境检测.md) → [2](阶段二-Hermes多代理系统.md) → [3](阶段三-网络代理.md) → [4](阶段四-输入法.md) → [5](阶段五-Sunshine串流.md) | 参考：[环境要求](环境要求.md) [游戏环境](游戏环境.md) [踩坑记录](踩坑记录.md)

---

# 阶段三：网络代理

## v4 方案：纯 Clash Verge Rev

### 架构

```
Hermes Gateway (systemd user service)
  ↓ EnvironmentFile
  ↓ HTTP_PROXY=http://127.0.0.1:7897（兼容）
  ↓ ALL_PROXY=socks5://127.0.0.1:7897（防 DNS 污染）
  ↓
Clash Verge Rev (system service, TUN 模式)
  ↓ mixed-port 7897（同时支持 HTTP + SOCKS5）
  ↓
互联网
```

### 代理策略

| 协议 | 用途 |
|---|---|
| HTTP (`http://127.0.0.1:7897`) | 兼容所有工具（curl/pip/git） |
| SOCKS5 (`socks5://127.0.0.1:7897`) | 敏感应用（Thunderbird/Discord），**远程 DNS 防污染** |

> Clash Verge 7897 端口同时支持两种协议，不用二选一。

### Drop-in 文件

**`~/.config/systemd/user/hermes-gateway.service.d/10-proxy.conf`**：

```ini
[Service]
EnvironmentFile=/home/chao/.config/systemd/user/hermes-gateway.proxy.env
After=clash-verge-service
Wants=clash-verge-service
```

**`~/.config/systemd/user/hermes-gateway.proxy.env`**：

```bash
HTTP_PROXY=http://127.0.0.1:7897
HTTPS_PROXY=http://127.0.0.1:7897
http_proxy=http://127.0.0.1:7897
https_proxy=http://127.0.0.1:7897
ALL_PROXY=socks5://127.0.0.1:7897
all_proxy=socks5://127.0.0.1:7897
NO_PROXY=127.0.0.1,localhost,::1
no_proxy=127.0.0.1,localhost,::1
```

### 验证连通

```bash
# HTTP 代理测试
curl -fsS --max-time 8 -x http://127.0.0.1:7897 -o /dev/null -w '%{http_code}\n' http://www.gstatic.com/generate_204
# 期望：204

# SOCKS5 代理测试
curl -fsS --max-time 8 --socks5-hostname 127.0.0.1:7897 -o /dev/null -w '%{http_code}\n' http://www.gstatic.com/generate_204
# 期望：204
```

## 敏感应用代理配置

### Discord

`~/.local/bin/discord-proxy`：

```bash
#!/bin/bash
set -e
PROXY_URL="socks5://127.0.0.1:7897"
export HTTP_PROXY="$PROXY_URL"
export HTTPS_PROXY="$PROXY_URL"
export ALL_PROXY="$PROXY_URL"
exec /usr/bin/discord --proxy-server="$PROXY_URL" "$@"
```

### Thunderbird

`~/.local/bin/thunderbird-proxy`：同上 wrapper

Thunderbird profile 的 `user.js`：

```javascript
user_pref("network.proxy.type", 1);
user_pref("network.proxy.socks", "127.0.0.1");
user_pref("network.proxy.socks_port", 7897);
user_pref("network.proxy.socks_version", 5);
user_pref("network.proxy.socks_remote_dns", true);
user_pref("network.proxy.no_proxies_on", "localhost,127.0.0.1,::1,.local");
```

> ⚠️ `socks_remote_dns: true` 是关键——让 DNS 也在远程解析，防止污染。

## SOCKS5 vs HTTP 代理

| 特性 | HTTP CONNECT | SOCKS5 |
|---|---|---|
| DNS 解析位置 | 本地（可能被污染）| **远程** ✅ |
| 抗 DNS 污染 | ❌ | ✅ |
| UDP 支持 | ❌ | ✅ |
| 协议支持 | 只 HTTP | 任意 TCP/UDP |

**结论**：全部配上，HTTP 做兼容兜底，SOCKS5 做安全主力。
