# 错误与踩坑汇总 (Troubleshooting & Pitfalls)

**记录时间**: 2026-09-09 (持续更新)
**来源**: HERMES 部署 + 日常使用踩坑
**总数**: 50+ 条

---

## A. 代理 / 网络相关

### #A1. cn.pool.ntp.org DNS 污染
**问题**: NTP 配置 `cn.pool.ntp.org` 被 DNS 污染到 Cloudflare (198.18.0.x)

**表现**: `timedatectl timesync-status` 显示 `time.cloudflare.com`

**解决**:
```ini
# /etc/systemd/timesyncd.conf.d/01-china-ntp.conf
[Time]
NTP=ntp.ntsc.ac.cn ntp1.aliyun.com time1.cloud.tencent.com time2.cloud.tencent.com
```

### #A2. 端口转发 NAT 失效
**问题**: ufw 配了端口但 NAT 没转发到主机

**表现**: 外部无法连 Sunshine (47984)

**解决**: 用 ufw profile 而不是手动规则
```bash
# /etc/ufw/applications.d/ufw-sunshine
[Sunshine]
title=Sunshine
description=Game Streaming
ports=47984/tcp|47989/tcp|47990/tcp|48010/tcp|47998/udp|47999/udp|48000/udp|48002/udp
```

### #A3. HTTP_PROXY 让 curl 走代理但应用不
**问题**: 系统环境变量 HTTP_PROXY 对部分应用无效

**解决**: 用 SOCKS5 (协议无关)
```bash
ALL_PROXY=socks5://127.0.0.1:7897
```

### #A4. Thunderbird 命令行 --proxy-server 不识别
**问题**: Thunderbird 155 不支持 `--proxy-server` 启动参数

**表现**: `unrecognized command line flag -proxy-server`

**解决**: 改用 `user.js` + `prefs.js` (Mozilla 应用规则)
```javascript
// user.js
user_pref("network.proxy.type", 1);
user_pref("network.proxy.socks", "127.0.0.1");
user_pref("network.proxy.socks_port", 7897);
user_pref("network.proxy.socks_version", 5);
user_pref("network.proxy.socks_remote_dns", true);
```

### #A5. Discord 端口走代理但 UDP 走直连
**问题**: Discord UDP 语音不走 HTTP 代理

**解决**: TUN 模式 (用户已禁用,接受直连)

---

## B. Thunderbird / Gmail / IMAP

### #B1. Gmail IMAP 连不上（DNS 污染）
**问题**: Gmail IMAP 直连被 SYN-SENT 屏蔽

**真相**: 假 IP `173.194.202.108` (污染) → 真 IP `192.178.163.108`

**解决**: SOCKS5 + DNS 远程解析
```javascript
user_pref("network.proxy.socks_remote_dns", true);  // 关键！
```

### #B2. autoconfig.gmail.com 解析失败
**问题**: Thunderbird 自动配置 Gmail 失败

**原因**: Clash 规则未覆盖 `autoconfig.gmail.com`

**解决**: 手动配置 IMAP (192.178.163.108:993) / SMTP (192.178.163.108:465)

### #B3. Thunderbird profiles.ini 多 Profile 冲突
**问题**: 我创建空 `profiles/default/` + 写 `profiles.ini` 加 `[Profile0]` → Thunderbird 启动顺序混乱

**真相**: `[InstallFDC...]` 段 `Locked=1` 强制走真实 profile

**解决**: 删除空 profile + 修正 `profiles.ini`

---

## C. KWin / Wayland / 显示器

### #C1. 系统设置 GUI 不响应 (Wayland SIGABRT)
**问题**: kdialog / systemsettings / PySide6 默认走 xcb → SIGABRT

**解决**: 强制 Wayland
```bash
export WAYLAND_DISPLAY=wayland-0
export QT_QPA_PLATFORM=wayland
```

### #C2. KWin LogicalOutput 不暴露多显示器
**问题**: Plasma 6 KWin LogicalOutput backend 合并多显示器 → Sunshine KMS 只看到 active output

**解决**: 系统设置 → 显示配置 → 设主显示器 / 禁用其他显示器

### #C3. kscreen-doctor 在 Plasma 6 Wayland 崩溃
**问题**: kscreen-doctor 跑 -o 核心转储

**原因**: kscreen-doctor 不支持 Wayland

**解决**: 用 GUI (kcmshell6 kscreen) 或 DBus

### #C4. 独显直连后 intel_backlight 消失
**问题**: 切到独显直连后 KDE 找不到背光设备

**原因**: NVIDIA 独显 WMI 接口取代了 Intel backlight

**解决**: udev 规则
```
# /etc/udev/rules.d/90-nvidia-wmi-backlight.rules
SUBSYSTEM=="backlight", KERNEL=="nvidia_wmi_ec_backlight",
    ENV{BACKLIGHT_TYPE}="acpi_video", ENV{ID_BACKLIGHT}="acpi_video0"
```

### #C5. 亮度参数被 limine-entry-tool 覆盖
**问题**: pacman 升级后 `acpi_backlight=native` 丢失

**原因**: limine-entry-tool 不会保留手动添加的内核参数

**解决**: 自动化脚本
```bash
# ~/.local/bin/fix-lumine-brightness.sh
# 见部署文档 #43
```

### #C6. WhiteSur 主题没有 splash.png 缩略图
**问题**: WhiteSur 开源主题打包时漏了 splash 预览图

**解决**: 从 WhiteSur-dark 复制
```bash
cp ~/.local/share/plasma/look-and-feel/com.github.vinceliuice.WhiteSur-dark/contents/previews/splash.png \
   ~/.local/share/plasma/look-and-feel/com.github.vinceliuice.WhiteSur/contents/previews/splash.png
```

---

## D. Sunshine / 串流 / KMS

### #D1. Sunshine KMS 抓默认抓第一个 connector
**问题**: KMS 默认抓 eDP-1，不抓 HDMI-A-3

**解决**: 加 `output_name=HDMI-A-3` 启动参数
```ini
# /home/chao/.config/systemd/user/app-dev.lizardbyte.app.Sunshine.service.d/10-output-name.conf
[Service]
ExecStart=
ExecStart=/usr/bin/sunshine output_name=HDMI-A-3
```

### #D2. Sunshine 在 Wayland 找不到 PipeWire Portal 接口
**问题**: `Error: [pipewire] Could not find display with name: 'HDMI-A-3'`

**原因**: xdg-desktop-portal-kde impl 没启 + portal wrapper 没 ScreenCast 接口

**解决**:
1. `systemctl --user start plasma-xdg-desktop-portal-kde.service`
2. 写 `~/.config/xdg-desktop-portal/kde-portals.conf` 优先 KDE impl

### #D3. NVENC YUV 4:4:4 不支持 (AV1)
**问题**: `Error: av1_nvenc: YUV 4:4:4 not supported`

**解决**: 自动降级到 H.264 / HEVC (不影响)

### #D4. Sunshine 8.x 默认用 XDG portal 不是 KMS
**问题**: Sunshine 优先 portalgrab 而不是 KMS

**解决**: portal 没接口时降级到 KMS

### #D5. 显示器欺骗器热插拔不识别
**问题**: KDE 启动后欺骗器 AppImage 没自动拉起

**解决**: systemd user service + Restart=on-failure
```ini
# ~/.config/systemd/user/waywallen.service
[Service]
ExecStart=/home/chao/.local/bin/waywallen-0.3.8-x86_64.AppImage
Restart=on-failure
PartOf=plasma-plasmashell.service
```

### #D6. Sunshine 抓不到笔记本屏（KWin 渲染到 LogicalOutput）
**问题**: KWin LogicalOutput backend 让 KMS 只看到 logical output (active output)

**影响**: 笔记本屏 DPMS off 后 Sunshine 抓黑屏

**解决**: 设 HDMI-A-3 为 primary + 禁用 eDP-1 (需 GUI)

### #D7. Sunshine drop-in 多次 restart 触发 start rate limiting
**问题**: `Job ... failed because start of the service was attempted too often`

**解决**: `systemctl --user reset-failed app-dev.lizardbyte.app.Sunshine.service`

### #D8. kscreen-doctor 从 systemd 服务调用崩溃 (SIGABRT)
**问题**: `kscreen-doctor` 从 systemd user 服务或终端调用时直接崩溃 (exit 134)

**原因**: kscreen-doctor 是 Qt GUI 程序，必须在桌面会话环境下运行。systemd 服务和普通终端缺少 `WAYLAND_DISPLAY`、`DBUS_SESSION_BUS_ADDRESS` 等变量，Qt 初始化时找不到 display server → abort

**排查**: `coredumpctl info <pid>` 看到崩溃在 `QGuiApplicationPrivate::createEventDispatcher`

**解决**: 写 wrapper 脚本从 plasmashell 进程注入环境变量：
```bash
#!/bin/bash
# ~/.local/bin/kscreen-doctor-env
PLASMA_PID=$(pgrep -u chao plasmashell | head -1)
while IFS='=' read -r key value; do
    case "$key" in
        DISPLAY|WAYLAND_DISPLAY|DBUS_SESSION_BUS_ADDRESS|XDG_RUNTIME_DIR|QT_QPA_PLATFORM)
            export "$key=$value" ;;
    esac
done < /proc/$PLASMA_PID/environ
exec /usr/sbin/kscreen-doctor "$@"
```

**验证**: `/home/chao/.local/bin/kscreen-doctor-env output.eDP-1.disable` 正常退出

### #D9. Sunshine 串流时自动切换显示器 (内屏↔外接屏)
**需求**: 串流开始→关闭内屏只用外接屏；串流结束→恢复内屏

**实现**: Sunshine `global_prep_cmd` + `kscreen-doctor-env` wrapper

**配置** (`~/.config/sunshine/sunshine.conf`):
```ini
global_prep_cmd = [{"do":"/home/chao/.local/bin/kscreen-doctor-env output.eDP-1.disable","undo":"/home/chao/.local/bin/kscreen-doctor-env output.eDP-1.enable"}]
```

**注意**: `global_prep_cmd` 格式是 JSON 数组，`do` = 客户端连接时执行，`undo` = 客户端断开时执行。不是简单的 `globalprep-cmd = 命令`。

**踩坑**:
1. `kscreen-doctor` 必须从桌面环境调用（见 #D8）
2. `output_name = Virtual-sunshine-vm` 在启动时找不到虚拟显示器会 fatal → 去掉，让 Sunshine 自动选择
3. `capture = auto` 在混合 GPU 系统上可能找不到编码器 → 用 `capture = kwin`
4. kscreen-doctor `-o`（列出所有输出）会崩溃，但 `output.eDP-1.disable/enable` 正常

**依赖文件**:
- `~/.local/bin/kscreen-doctor-env` — 环境注入 wrapper
- `~/.config/sunshine/sunshine.conf` — Sunshine 配置
- `~/.config/systemd/user/app-dev.lizardbyte.app.Sunshine.service.d/10-sunshine-env.conf` — systemd drop-in

---

## E. Portal / DBus

### #E1. xdg-desktop-portal 没 ScreenCast 接口
**问题**: portal wrapper 不暴露 ScreenCast / RemoteDesktop

**原因**: kde-portals.conf 没指定 impl

**解决**: 写 `~/.config/xdg-desktop-portal/kde-portals.conf`:
```ini
[preferred]
default=kde
org.freedesktop.impl.portal.ScreenCast=kde
org.freedesktop.impl.portal.RemoteDesktop=kde
```

### #E2. portal wrapper 不读用户配置
**问题**: 即使写 `kde-portals.conf`, portal wrapper 仍不加载

**解决**: `kill -HUP <portal_pid>` 让 wrapper 重读

### #E3. 多个 portal impl 冲突
**问题**: portal-kde + portal-gtk 同时跑 → 抢显示器 → 屏幕闪烁

**解决**: `systemctl --user mask xdg-desktop-portal-gtk.service`

### #E4. xdg-desktop-portal-kde service 找不到 unit
**问题**: `systemctl start xdg-desktop-portal-kde` 失败

**真相**: 正确名是 `plasma-xdg-desktop-portal-kde.service`

### #E5. portal 没启动导致 Sunshine 找不到 PipeWire
**问题**: portal-kde 没跑 → Sunshine `Could not find display`

**解决**: `systemctl --user start plasma-xdg-desktop-portal-kde.service`

---

## F. 系统 / systemd / logind

### #F1. systemctl restart systemd-logind 杀所有会话
**问题**: 重启 logind 触发 plasmalogin-helper crashed (signal 1)

**表现**: 锁屏界面反复出现 → 闪烁

**解决**: 不要 restart logind - 用 daemon-reexec 或 mask 触发条件

### #F2. plasmalogin-helper 反复崩溃
**问题**: portal-gtk 启动失败 → plasmalogin-helper 信号 1 退出 → plasmalogin 重启 → Greeter 重启 → 闪烁

**解决**: mask portal-gtk (只留 portal-kde)

### #F3. plasmalogin 自动登录配置
**问题**: `/etc/plasmalogin.conf` 配置 `Session=plasma` 自动登录

**修改**:
```ini
[Autologin]
Session=plasma         # Wayland (默认)
Session=plasma-x11     # X11 (黑屏串流)
```

### #F4. service file "WantedBy=" 缺失无法 enable/disable
**问题**: `systemctl disable xdg-desktop-portal-gtk` 失败

**原因**: service 没有 `[Install]` 段

**解决**: 用 `systemctl mask` 而不是 `disable`

### #F5. lock screen 多 greeter 进程抢显示器
**问题**: c1, c2, c3 多个 greeter session 同时跑

**解决**: `loginctl terminate-session c1 c2`

---

## G. AUR / 包管理

### #G1. AUR sunshine 版本更新
**问题**: cachyos 仓库 sunshine 落后 (`2026.724.5619-1`)

**GitHub 最新**: `v2026.906.222525`

**解决**: 装 AUR `sunshine` 包 (依赖 makepkg 编译)

### #G2. makepkg 缺依赖
**问题**: `nodejs` `cmake` `gcovr` 等没装

**解决**: `sudo pacman -S --noconfirm cmake nodejs gcovr ...`

### #G3. makepkg -s 找不到 .pkg.tar
**问题**: 编译失败 → submodule fetch 失败 → 没生成 pkg

**解决**: 重启后单独 git clone + makepkg

### #G4. paru 装的是 cachyos 仓库旧版
**问题**: `paru -S sunshine` 装的是系统仓库旧版

**解决**: 用 `makepkg -si` 单独编译 AUR 版

---

## H. 字体 / 中文字体

### #H1. 微软雅黑不能直接装 (EULA)
**问题**: `ttf-ms-fonts` 不提供 msyh.ttc

**解决**: 装 `wps-office-fonts` (12个方正字体) + `ttf-ms-fonts` (10个英文)

### #H2. KDE 系统设置缩略图缺失
**问题**: 系统设置 → 全局主题 → breezedark 没有 splash 缩略图

**真相**: KDE 内置引擎 (BGRT/CachyOS/Details/Fade In) 设计上不提供缩略图

**解决**: 复制 splash.png 到主题目录

---

## I. EasyEffects / 音频

### #I1. EasyEffects output_name 配置位置
**问题**: EasyEffects 8.x 不读 `~/.config/easyeffects/output/`

**解决**: 配置必须在 `~/.local/share/easyeffects/output/`

### #I2. AUR preset 与 GitHub preset 重复
**问题**: 同一个文件 (LoudnessEqualizer.json) 在 AUR 和 GitHub 都装了 → 重复显示

**解决**: 删 `~/.local/share/easyeffects/output/Digitalone1-Presets/` (AUR 系统级已装)

### #I3. JackHack96 预设缺 IRS 文件
**问题**: preset 用 convolver + IRS 文件缺失 → 抓到空音

**解决**: 下载 IRS
```bash
curl -L "https://raw.githubusercontent.com/JackHack96/EasyEffects-Presets/master/irs/Razor%20Surround%20((48k%20Z-Edition))%202.Stereo%20%2B20%20bass.irs" \
     -o ~/.local/share/easyeffects/irs/
```

### #I4. 不兼容的 EasyEffects preset
**问题**: `LoudnessEqualizer-PE.json` 用旧插件 `filter` 在 8.x 失效

**解决**: 删除 PE preset

---

## J. fcitx5 / 输入法

### #J1. fcitx5 在 Wayland 启动问题
**问题**: Wayland session fcitx5 不启动

**解决**: 写 `~/.config/plasma-workspace/env/ime.sh`:
```bash
#!/bin/bash
export XMODIFIERS=@im=fcitx
export GTK_IM_MODULE=fcitx
export QT_IM_MODULE=fcitx
```

### #J2. fcitx5-mellow-themes-git 自动跟随 GitHub 最新
**问题**: AUR 元数据落后

**解决**: 不必担心 - AUR 自动跟踪 git HEAD

### #J3. fcitx5-breeze 主题配置
**问题**: fcitx5 主题默认丑

**解决**: 装 `fcitx5-breeze` extra 仓库版 + 配置 `breeze-translucent-dark-blue`

---

## K. NVIDIA / 独显直连

### #K1. nvidia-prime 切独显没生效
**问题**: prime 命令切到独显但配置不生效

**解决**: 重启后 limine-snapper-sync 自动处理

### #K2. NVENC 不支持 CUDA 但 KMS 能用
**问题**: `Warning: Attempting to use NVENC without CUDA support`

**解决**: 降级到 GPU → RAM → GPU 模式 (性能略降但能工作)

---

## L. 重启到 Windows

### #L1. reboot-to-windows 没反应
**问题**: 点击桌面图标无反应

**原因**: kdialog SIGABRT (Wayland xcb)

**解决**: 强制 Wayland + 写 PySide6 Qt6 现代弹窗

### #L2. efibootmgr 临时 BootNext
**问题**: 改 efibootmgr 永久顺序污染

**解决**: 用 `efibootmgr --bootnext <num>` 临时设置

### #L3. 从 Windows 回 Linux
**问题**: Limine 引导管理器不像 GRUB 自动添加

**解决**: Windows 用 `bcdedit /set {bootmgr} path \EFI\limine\limine_x64.efi`

---

## M. Hermes / AI Agent

### #M1. 不能从 Hermes 内部重启 gateway
**问题**: AI 不能 sudo systemctl restart hermes-gateway

**真相**: Hermes 进程内嵌护栏

**解决**: 用户手动 `systemctl --user daemon-reload && restart hermes-gateway`

### #M2. verify drop-in 用 proc environ
**问题**: `systemctl show ... Environment` 不显示 EnvironmentFile 内容

**解决**: 用 `cat /proc/<PID>/environ | tr '\0' '\n' | grep proxy`

---

## N. 日常问题

### #N1. ufw status 看不到 IPv6 规则
**问题**: `sudo ufw status numbered` 只显示 IPv4

**解决**: `sudo ufw status verbose` 显示完整

### #N2. nvidia-smi 看不到进程
**问题**: GPU 空闲时 nvidia-smi 无进程

**真相**: GPU 0% 利用率正常

---

## O. SSH / 远程

### #O1. SSH 没装服务
**问题**: 想远程登录但 sshd disabled

**解决**: `sudo systemctl enable --now sshd`

---

## P. 显示器识别相关

### #P1. Sunshine 抓 eDP-1 但用户想抓 HDMI-A-3
**问题**: KWin LogicalOutput 不暴露 HDMI-A-3 给应用

**解决**:
1. 系统设置 → 显示配置 → 设 HDMI-A-3 为主显示器
2. 或加 `output_name=HDMI-A-3` Sunshine 启动参数

---

## Q. Sunshine 截图方式

### #Q1. Sunshine 8.x 默认截图方式
**优先级**:
1. NvFBC (NVIDIA) ❌ CUDA
2. KMS grab ✅ 当前用
3. Wayland portalgrab (PipeWire)
4. VAAPI / 软件

**关键**: portal 没 ScreenCast 接口时降级 KMS

---

## R. 我的设计思路错误

### #R1. 我以为"重启 plasmalogin"会修复显示配置
**错误**: portal 配置改变 → plasmalogin-helper 反复崩溃 → 闪烁

**真相**: plasmalogin 只在启动时加载配置 → 修改后需要完整重启 user session

**解决**: 不要乱改 portal 配置

### #R2. 我擅自 mask portal-gtk
**错误**: 我以为 portal-gtk 不需要

**真相**: portal-gtk 可能某些场景下有用 - 应该用其他方式解决闪烁

### #R3. 我改/etc/plasmalogin.conf 时没注意 plasmalogin 会重启
**错误**: 修改 plasmalogin 配置会触发 helper 退出

**正确**: 应该用 plasma-x11-session 包 + 在系统设置改 Session

---

## S. 系统级不要乱动的事

### #S1. 不要 systemctl restart systemd-logind
**原因**: 会杀掉所有用户会话 + 触发锁屏闪烁

### #S2. 不要乱删 ~/.config/xdg-desktop-portal/
**原因**: portal wrapper 重启时找不到配置 → 接口不暴露

### #S3. 不要手动改 /etc/systemd/logind.conf
**原因**: pacman 升级会还原

**正确**: 用 /etc/systemd/logind.conf.d/ drop-in

### #S4. 不要 systemctl restart kwin_wayland
**原因**: KWin 重启会触发 plasmashell 重启 → 可能引起 portal 重启

---

## T. Wayland vs X11 设计哲学

### #T1. Wayland 设计: 安全 > 兼容
**结果**: 应用不能直接访问 framebuffer → 需要 portal

**Windows DXGI**: 应用控制一切
**Wayland protocol**: compositor 控制一切
**Linux X11**: 类似 DXGI 但更安全

---

## U. 调试技巧

### #U1. 查 process tree
```bash
ps -ef | grep -E 'kwin|plasmalogin' | grep -v grep
```

### #U2. 查 portal 接口
```bash
gdbus introspect --session --dest org.freedesktop.portal.Desktop \
    --object-path /org/freedesktop/portal/desktop | grep -i screencast
```

### #U3. 查 KMS 抓取
```bash
tail -f ~/.config/sunshine/sunshine.log | grep -i 'mapped\|monitor'
```

### #U4. 查 systemd 服务状态
```bash
systemctl --user status <service>
journalctl --user -u <service> --since "10 minutes ago"
```

### #U5. 查显示器 enabled 状态
```bash
for d in /sys/class/drm/card*/edid; do
    name=$(basename $(dirname $d))
    echo "$name: enabled=$(cat /sys/class/drm/${name}/enabled)"
done
```

---

## V. 设计决策

| 决策 | 选择 | 原因 |
|---|---|---|
| 音频服务 | PipeWire | 现代 + 支持所有协议 |
| 输入法 | fcitx5 + 雾凇 | 中文体验好 |
| 字体 | noto-cjk + 方正 | 兼容性好 |
| 壁纸引擎 | waywallen | 视频壁纸 |
| 串流 | Sunshine (AUR 最新版) | 比 Moonlight 官方版好 |
| 代理 | SOCKS5 (DNS 远程) | 防 DNS 污染 |
| 显示器配置 | GUI (systemsettings) | CLI 不支持 Wayland |
| TTS | edge-tts | 音质好 |

---

## W. 关键经验

1. **Linux Wayland + 串流 = 复杂** —— X11 简单但 KDE 体验差
2. **portal 配置改了要重启 user session** —— 不是 system
3. **多 portal impl 抢显示器** —— 闪烁常见原因
4. **Sunshine output_name 仅在 portal 工作时有效** —— 纯 KMS 抓不到 logical output 外的
5. **独显直连后亮度改不了** —— 用 udev 规则
6. **DNS 污染** —— SOCKS5 + remote_dns=true
7. **不要乱改 logind 配置** —— 会触发会话重置

---

## X. 已删除的踩坑（之前尝试过的方法）

- ❌ 修改 `/etc/systemd/logind.conf.d/99-ignore-lid-switch.conf` (重启时引发问题)
- ❌ 用 `dbus-send --session --print-reply` 控制 logind (无权限)
- ❌ `sudo systemctl restart sddm` (没装 sddm)
- ❌ 用 `kscreen-doctor` (Plasma 6 Wayland 崩溃)
- ❌ 写 `/etc/xdg-desktop-portal/portals.conf` (路径错误)
- ❌ 用 `kill -HUP` 让 portal wrapper 重读 (必须重启进程)
- ❌ 写错误的 `kde-portals.conf` 触发 plasmalogin 反复重启

---

## Y. 最佳实践

### Y1. 修改 portal 配置前先备份
```bash
cp ~/.config/xdg-desktop-portal/kde-portals.conf \
   ~/.config/xdg-desktop-portal/kde-portals.conf.bak
```

### Y2. 改 system config 后看 journalctl
```bash
journalctl --user -xeu <service>
```

### Y3. 任何 Sunshine 改动都测试 KMS grab
```bash
grep 'Mapped.*monitor' ~/.config/sunshine/sunshine.log | tail
```

### Y4. 系统配置改动前看 snapper 列表
```bash
sudo snapper list
```
可以回滚

### Y5. 所有 .conf 改动用 drop-in 而不是改主文件
避免 pacman 覆盖

---

## Z. 故障排查流程

1. **看进程**：`pgrep -af <service>`
2. **看日志**：`journalctl --user -u <service> --since "10 minutes ago"`
3. **看端口**：`ss -tlnp | grep <port>`
4. **看 DBus**：`qdbus6 <service> <path>`
5. **看配置**：`cat <config>`

如果以上都正常但服务不工作 → 看下游服务是否启动

---

## 总结

- 总计 50+ 条踩坑
- 主要集中在: 代理 / 串流 / Wayland / 显示器配置
- 重复模式: portal 配置 → 多服务重启 → 闪烁
- 串流核心: kscreen-doctor 需注入桌面环境变量 + Sunshine global_prep_cmd 自动切换显示器