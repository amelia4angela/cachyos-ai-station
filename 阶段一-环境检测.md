# 阶段一：环境检测

部署前先检测当前环境，确认所有前置条件满足。

## 系统环境检测

```bash
# CachyOS 版本
cat /etc/os-release | head -5

# 内核版本
uname -r

# KDE Plasma 版本
plasmashell --version

# Wayland 状态
echo $WAYLAND_DISPLAY

# systemd 版本
systemctl --version | head -1

# Linger 状态
loginctl show-user $USER | grep Linger
```

## Hermes 环境检测

```bash
# Hermes 版本
~/.hermes/hermes-agent/venv/bin/hermes --version

# 配置文件
test -f ~/.hermes/config.yaml && echo "config.yaml 存在" || echo "不存在"

# 已有技能
ls ~/.hermes/skills/

# Gateway 状态
systemctl --user status hermes-gateway.service
```

## 网络环境检测

```bash
# Clash Verge 状态
systemctl is-active clash-verge-service    # 应为 active
systemctl is-enabled clash-verge-service   # 应为 enabled

# 7897 端口监听
ss -tlnp | grep :7897

# Clash 配置（确认节点已配置）
head -20 ~/.local/share/io.github.clash-verge-rev.clash-verge-rev/clash-verge.yaml

# SkillHub 是否已安装
which skillhub
```

## 输入法环境检测

```bash
# fcitx5
which fcitx5
which fcitx5-configtool

# 已装输入法包
pacman -Q | grep -E "fcitx5|rime|opencc"

# 环境变量
echo "XMODIFIERS=$XMODIFIERS"
echo "GTK_IM_MODULE=$GTK_IM_MODULE"
echo "QT_IM_MODULE=$QT_IM_MODULE"
```

## 输出报告

AI 会将检测结果整理成表格，标注每项的状态（✅/❌）和决策，然后进入阶段二。
