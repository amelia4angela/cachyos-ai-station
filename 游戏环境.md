# 游戏环境

## 硬件

| 项目 | 规格 |
|---|---|
| GPU | NVIDIA RTX 4060 Max-Q (AD107M) |
| 驱动 | nvidia-utils 610.57.04 |
| iGPU | Intel Raptor Lake UHD Graphics |
| 内核 | linux-cachyos 7.2.3 |

## 已安装组件

### GPU 与图形

| 包名 | 用途 |
|---|---|
| `nvidia-utils` | NVIDIA 驱动核心 |
| `nvidia-settings` | NVIDIA 控制面板 |
| `nvidia-prime` | Optimus 双显卡切换 |
| `vulkan-intel` / `vulkan-icd-loader` | Vulkan 支持 |
| `mesa` | 开源 GPU 驱动 |
| `lib32-nvidia-utils` | 32位 NVIDIA（Steam 游戏）|

### 游戏平台

| 包名 | 用途 |
|---|---|
| `steam` | Valve Steam 平台 |
| `lutris` | Linux 游戏管理器 |
| `heroic-games-launcher-bin` | Epic/GOG 启动器 |
| `wine` / `wine-cachyos-opt` | Windows 兼容层 |
| `proton-cachyos-slr` | CachyOS 优化版 Proton |
| `protontricks` / `winetricks` | Proton/DLL 管理 |
| `protonup-qt` | Proton 版本管理 GUI |
| `umu-launcher` | Proton 启动器 |

### 性能工具

| 包名 | 用途 |
|---|---|
| `mangohud` | 性能叠加层（FPS/CPU/GPU） |
| `goverlay` | MangoHud GUI 配置 |
| `gamescope` | Valve 游戏合成器 |
| `gamemode` | Feral 游戏模式（CPU/GPU 提频）|
| `nvtop` | GPU 使用率监控 |

### 音频 & 手柄

| 包名 | 用途 |
|---|---|
| `pipewire` + `pipewire-pulse` | 音频服务器 |
| `lib32-libpulse` | 32位音频（老游戏）|
| `steam-devices` | 手柄 udev 规则 |
| `joydev` | 内核手柄驱动 |

## 常用命令

```bash
# MangoHud 叠加层
mangohud %command%

# GameMode 优化
gamemoderun %command%

# ProtonUp-Qt 管理 Proton 版本
protonup-qt
```
