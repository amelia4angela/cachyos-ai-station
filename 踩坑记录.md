> [Home](README.md) | 部署指南：[0](阶段零-前置授权.md) → [1](阶段一-环境检测.md) → [2](阶段二-Hermes多代理系统.md) → [3](阶段三-网络代理.md) → [4](阶段四-输入法.md) → [5](阶段五-Sunshine串流.md) | 参考：[环境要求](环境要求.md) [游戏环境](游戏环境.md) [踩坑记录](踩坑记录.md)

---

# 踩坑记录

部署过程中遇到的问题和解决方案。

---

## #45 SkillHub 部分技能 404

**现象**：
```
skillhub install pdf --namespace clawhub_kirkraman → 404
skillhub install find-skills --namespace clawhub_penglovemeng → 404
```

**解决方案**：
- `pdf`：Hermes 自带，跳过
- `find-skills`：用 `clawhub_fangkelvin/find-skills-skill`

---

## #46 Phase 0 未执行导致 paru 缺失

**现象**：`paru -S` 报 `paru: 未找到命令`

**解决方案**：直接帮装 paru

---

## Clash TUN Fake-IP 劫持 NTP DNS

**现象**：`ntp.ntsc.ac.cn` 解析到 `198.18.0.20`（fake-ip）

**解决方案**：用 IP 直连 NTP 服务器，绕过 DNS

```bash
NTP=113.141.164.38 113.141.164.39 1.82.219.234
```

---

## `sudo -S` 密码管道被 Hermes 拦截

**现象**：`sudo -S pacman ...` 触发 `BLOCKED: sudo password guessing`

**解决方案**：配 NOPASSWD 免密 sudo，不需要 `-S`

---

## `systemctl restart hermes-gateway` 被拦截

**现象**：Hermes 内部护栏阻止重启自身

**解决方案**：用户在另一个终端执行重启命令

---

## `hermes config set` 双重引号

**现象**：值带外层引号变成 `"\"小涵\""`

**解决方案**：值不带外层引号：`hermes config set key 'value'`

---

## `skillhub --dir` 必须是顶层参数

**现象**：`skillhub search xxx --dir path` 报 `unrecognized arguments`

**正确写法**：`skillhub --dir path search xxx`

---

## `skillhub search` 多 namespace 同名

**现象**：同名技能在多个 namespace 下都有

**解决方案**：先 search 看输出，照抄 `install: skillhub install <slug> --namespace <ns>`

---

## Sunshine capture = kwin 黑屏

**现象**：用 `capture = kwin` + NVENC 时，Moonlight 连接后黑屏，日志报 `Couldn't scale frame: 无效的参数`。

**根因**：KWin/PipeWire 输出的帧格式（YUV 4:4:4）与 NVENC 编码器不兼容。

**解决方案**：改用 `capture = kms`（DRM framebuffer 直接捕获），绕过 PipeWire 格式问题。

---

## Sunshine capture = gpu 找不到显示

**现象**：用 `capture = gpu` 时，日志报 `Fatal: Unable to find display or encoder during startup`。

**根因**：GPU 捕获需要直接访问显示输出，当显示被 kscreen-doctor 禁用后不可用。

**解决方案**：改用 `capture = kms`。

---

## kscreen-doctor 崩溃（SIGABRT）

**现象**：`kscreen-doctor -o` 或 `kscreen-doctor output.eDP-1.disable` 报 `已中止（核心已转储）`。

**根因**：kscreen-doctor 二进制文件损坏或版本不兼容。

**解决方案**：重装 kscreen：`sudo pacman -S --needed kscreen`

---

## Sunshine 不登录无法串流

**现象**：重启后不进桌面，Moonlight 无法连接 Sunshine。

**根因**：Wayland 下没有登录就没有图形会话，Sunshine 需要显示才能捕获。

**解决方案**：Sunshine 通过 systemd user service + linger 开机自启，登录后自动可用。
