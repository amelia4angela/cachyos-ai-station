# 阶段零：前置授权

在部署开始前，用户需要在另一个终端执行以下命令。

## 1. 安装 AUR 助手（paru）

```bash
sudo pacman -S --needed --noconfirm base-devel git
git clone https://aur.archlinux.org/paru.git /tmp/paru
(cd /tmp/paru && makepkg -si --noconfirm)
rm -rf /tmp/paru
```

验证：
```bash
paru --version
```

## 2. 配置免密 sudo

```bash
echo 'chao ALL=(ALL) NOPASSWD: ALL' | sudo tee /etc/sudoers.d/99-chao-nopasswd
sudo chmod 440 /etc/sudoers.d/99-chao-nopasswd
sudo -n true && echo "✅ sudo ok"
```

> ⚠️ 免密 sudo 配好后，AI 执行 sudo 命令不需要密码。但装/删包、改系统配置仍会先问用户确认。

## 3. 开启 Linger

```bash
sudo loginctl enable-linger $USER
loginctl show-user $USER | grep Linger
# 应输出：Linger=yes
```

Linger 让 systemd user 服务在用户未登录时也能运行（Hermes gateway 需要）。

## 4. 无需提供订阅链接

Clash Verge 的订阅配置在 GUI 里完成，不是部署脚本的工作。脚本只负责把 Hermes drop-in 指向 Clash 的 7897 端口。

## 完成后

告诉 AI "零阶段 OK"，AI 进入阶段一。
